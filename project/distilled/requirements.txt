# Minimal dependency set for the distilled project.
# Five packages. Nothing here needs a GPU-specific build.

sentence-transformers>=3.0   # encoder, MultipleNegativesRankingLoss, trainer, model.encode()
datasets                     # one-line loading of sentence-transformers/askubuntu
numpy                        # the retrieval matmul (corpus @ query) and the two metrics
rank_bm25                    # the BM25 lexical baseline (~15 lines to use)
matplotlib                   # two figures: bar chart + line chart

# Not needed: faiss, transformers (comes with sentence-transformers), torch (ditto),
# beir, ir_datasets, pytorch-lightning, wandb, hydra, docker.
#
# If rank_bm25 will not install, replace the BM25 baseline with:
#   from sklearn.feature_extraction.text import TfidfVectorizer
# and cosine similarity via a sparse matmul. Say so in the report.
